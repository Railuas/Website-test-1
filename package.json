{
  "name": "kaus-news",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Build successful' && exit 0",
    "start": "echo 'Server started' && exit 0"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "cheerio": "^1.0.0-rc.12",
    "mongodb": "^5.7.0"
  }
}